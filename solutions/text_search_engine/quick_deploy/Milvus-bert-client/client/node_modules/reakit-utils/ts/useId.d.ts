import * as React from "react";
export declare type IdProviderProps = {
    children: React.ReactNode;
    unstable_prefix?: string;
};
export declare function IdProvider({ children, unstable_prefix: prefix }: IdProviderProps): JSX.Element;
export declare function useId(prefix?: string): string;
